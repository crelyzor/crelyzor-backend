/**
 * @deprecated
 * This file is superseded by scheduleService.ts (Phase 1.2 v2).
 * Availability is now scoped to AvailabilitySchedule (not User directly).
 * The old /scheduling/availability routes have been replaced with
 * /scheduling/schedules/:id/availability endpoints.
 *
 * This file is kept to avoid import errors from any external references but
 * exports no functions — delete it once all references are confirmed gone.
 */

export {};
